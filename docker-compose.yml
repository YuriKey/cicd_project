#docker-compose.yml
services:
  testrunner:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./reports:/app/reports
    depends_on:
      - selenoid
    environment:
      SELENIUM_PORT: 4444
    networks:
      - test-network

  selenoid:
    image: selenoid:latest-release
    volumes:
      - ./browsers.json:/etc/browsers.json
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "4444:4444"
    command: [
      "-conf", "/etc/browsers.json",
      "-limit", "5",
      "-auto-pull",
      "-service-startup-timeout", "2m",
      "-session-attempt-timeout", "2m"
    ]
    networks:
      - test-network

networks:
  test-network:
    driver: bridge
    name: test-network
